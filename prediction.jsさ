function predictNumbers(data){

    if(!data || data.length===0){
        document.getElementById("predictionResult").textContent =
        "データがありません";
        return;
    }

    const nums=[1,8,14,22,31,40];

    document.getElementById("predictionResult").textContent =
    nums.join(" ・ ");
}
